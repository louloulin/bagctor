{
  "name": "vscode-bactor-chat",
  "displayName": "Bactor Chat",
  "description": "High-performance chat plugin powered by Bactor",
  "version": "0.0.1",
  "publisher": "bactor",
  "engines": {
    "vscode": "^1.85.0"
  },
  "type": "commonjs",
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:vscode-bactor-chat.startChat",
    "onCommand:vscode-bactor-chat.connect"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "vscode-bactor-chat.startChat",
        "title": "Start Bactor Chat"
      },
      {
        "command": "vscode-bactor-chat.connect",
        "title": "Connect to Peer"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "bactor-chat",
          "title": "Bactor Chat",
          "icon": "resources/chat.svg"
        }
      ]
    },
    "views": {
      "bactor-chat": [
        {
          "type": "webview",
          "id": "bactorChatView",
          "name": "Chat"
        }
      ]
    }
  },
  "scripts": {
    "clean": "rimraf dist out *.vsix",
    "prebuild": "bun run clean && cd ../core && bun run build && cd ../web && bun run build",
    "build": "tsc -p ./ && bun build ./src/extension.ts --outdir ./dist --target node --external vscode",
    "watch": "tsc -p ./ -w",
    "lint": "eslint src --ext ts",
    "package": "vsce package --no-dependencies",
    "publish": "vsce publish --no-dependencies",
    "vscode:prepublish": "bun run build",
    "compile": "tsc -p ./",
    "pretest": "bun run compile && bun run lint",
    "test": "node ./out/test/runTest.js",
    "test-compile": "tsc -p ./",
    "dev": "bun run watch",
    "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=dist/extension.js --external:vscode --format=cjs --platform=node",
    "esbuild": "bun run esbuild-base --sourcemap",
    "esbuild-watch": "bun run esbuild-base --sourcemap --watch",
    "test-watch": "tsc -p ./ -w",
    "test-compile-watch": "tsc -p ./ -w"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "18.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.15.0",
    "@typescript-eslint/parser": "^6.15.0",
    "@vscode/test-electron": "^2.3.9",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "glob": "^11.0.1",
    "mocha": "^11.1.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@bactor/core": "workspace:*",
    "@bactor/web": "workspace:*"
  }
}